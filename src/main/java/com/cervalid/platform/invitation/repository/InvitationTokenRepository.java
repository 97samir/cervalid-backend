/*package com.cervalid.platform.invitation.repository;

import com.cervalid.platform.auth.token.ActivationToken;
import com.cervalid.platform.invitation.entity.InvitationToken;
import org.springframework.data.jpa.repository.JpaRepository;

import java.util.Optional;

public interface InvitationTokenRepository
        extends JpaRepository<InvitationToken, String> {

    Optional<InvitationToken> findByToken(String token);
}
*/
